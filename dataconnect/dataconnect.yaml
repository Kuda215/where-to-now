specVersion: "v1beta"
serviceId: "where-to-now"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "where-to-now-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
